@helper CreateBoard(int playerNumber,bool bot,int boardSize)
{
char a = 'A';
if (!bot)
{
        <p>Your board</p>
}
else
{
        <p>Bots board</p>
}
<table class="board" data-bot="@bot" data-board-size="@boardSize">
    @for (int i = 0; i < boardSize + 1; i++)
    {
        <tr class="tilerow" style="">
            @for (int j = 0; j < boardSize + 1; j++)
            {
                if (i == 0)
                {
                    if (j == 0)
                    {
                        <th class="tile"></th>
                    }
                    else
                    {
                        <th class="tile">@a</th>
                        a++;
                    }
                }
                else
                {
                    if (j == 0)
                    {
                        <th class="tile">@i</th>
                    }
                    else
                    {
                        <th class="tile" data-x="@j" data-y="@i" ship="none"></th>

                    }
                }

            }
        </tr>
    }
</table>
}

@helper ShipPlacingMenu(int[] shipTypes) {
    <p id="ship-placing-label">Place ships</p>
        for (int i = 0; i < 5; i++)
        {
            <span data-type="Label-ship.@i" data-amount="@shipTypes[i]" >@shipTypes[i]x</span>
            <table class="ship-placing">
                <tr>
                    @for (int j = 0; j < i + 1; j++)
                    {
                        <th class="choose-tile" data-disabled="False" data-type="@i"></th>
                    }
                </tr>
            </table>
        }
    <Button class="btn btn-success">Confirm Placement</Button>
    }
